#!/usr/bin/env bash

# current dir
currentDir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# chilopoda ui command
chilopodaCommand="import chilopodaqt; chilopodaqt.init()"

source $currentDir/chilopoda

